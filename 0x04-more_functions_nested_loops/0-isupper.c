#include "main.h"

/**
* _isupper - checks if int c is a lowercase letter,
* in ASCII.
* @c: is the variable name for the character.
* Return: 1 if c is lowercase, return 0 otherwise
*/

int _isupper(int c)
{
	return (c >= 65 && c <= 90);
}
