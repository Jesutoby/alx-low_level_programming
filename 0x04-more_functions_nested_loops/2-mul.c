#include "main.h"

/**
* mul - multiplies two integers.
* @a: is one of the integer variables,
* @b: is the other integer variable.
*
* Return: the product of the multiplication.
*/

int mul(int a, int b)
{
	return (a * b);
}
